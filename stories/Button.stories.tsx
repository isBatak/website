import { Button } from '@chakra-ui/react';
import { Meta } from '@storybook/react/types-6-0';

export default {
  title: 'Button',
} as Meta;

export const Primary = () => <Button size="lg">BUTTON</Button>;
